# Streamlit secrets 模板。
# 本地：复制为 .streamlit/secrets.toml 并填入你的 key（该文件被 .gitignore 排除）。
# Streamlit Community Cloud：在 App → Settings → Secrets 里粘贴下面这行。
# FRED 免费 key 申请：https://fred.stlouisfed.org/docs/api/api_key.html
# 留空也能用——宏观会自动回退到 ETF 代理（HYG/IEF、^TNX/^IRX）。
FRED_API_KEY = "在此填入你的_FRED_API_KEY"
